- route:
    id: route-5711
    from:
      id: from-3753
      uri: rest:get:/demo
      steps:
        - setBody:
            simple: 'Hello World1'
        - log:
            id: log-95
            message: "${body}"

- route:
    id: "api3"
    from:
      uri: "kafka:{{topicName.json}}?groupId={{consumergroup}}"
      steps:
        - log:
            id: log-223
            message: "${body}"